"use strict";
let emp = {
    id: 11,
    name: "manas",
    joined: (date) => {
        console.info(`Joining date of ${emp.name} is ${date}`);
    }
};
emp.joined(new Date());
//# sourceMappingURL=index.js.map